import 'package:flutter/material.dart';

class DosenScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Halaman Dosen'),
      ),
      body: Center(
        child: Text('Ini adalah halaman Dosen'),
      ),
    );
  }
}
